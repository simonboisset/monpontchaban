{
  "private": true,
  "sideEffects": false,
  "scripts": {
    "build": "prisma generate --data-proxy && node buildCron.js && tailwindcss -o ./app/styles/tailwind.css && remix build",
    "dev": "concurrently \"tailwindcss -o ./app/styles/tailwind.css --watch\" \"cross-env NODE_ENV=development netlify dev\" \"tsc --noEmit --watch\"",
    "start": "cross-env NODE_ENV=production netlify dev",
    "story": "concurrently \"tailwindcss -o ./app/styles/tailwind.css --watch\" \"ladle serve\"",
    "kill": "docker stop $(docker ps -aq) && docker rm $(docker ps -aq)",
    "up": "yarn upgrade-interactive --latest",
    "setup": "prisma generate dev",
    "generate": "prisma generate",
    "studio": "prisma studio",
    "reset": "prisma migrate reset --force",
    "docker": "docker-compose up -d",
    "seed": "prisma db seed",
    "ts": "tsc --noEmit",
    "test": "NODE_ENV=production yarn jest",
    "restart": "yarn kill && yarn docker && yarn setup && yarn dev"
  },
  "dependencies": {
    "@netlify/functions": "^1.0.0",
    "@prisma/client": "^3.15.2",
    "@remix-run/netlify": "^1.6.1",
    "@remix-run/node": "^1.6.1",
    "@remix-run/react": "^1.6.1",
    "cross-env": "^7.0.3",
    "dayjs": "^1.11.3",
    "expo-server-sdk": "^3.6.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "tiny-invariant": "^1.2.0"
  },
  "devDependencies": {
    "@remix-run/dev": "^1.6.1",
    "@remix-run/eslint-config": "^1.6.1",
    "@swc/core": "^1.2.205",
    "@swc/jest": "^0.2.21",
    "@types/jest": "^28.1.3",
    "@types/react": "^18.0.14",
    "@types/react-dom": "^18.0.5",
    "concurrently": "^7.2.2",
    "esbuild": "^0.14.47",
    "eslint": "^8.15.0",
    "jest": "^28.1.1",
    "prisma": "^3.15.2",
    "tailwindcss": "^3.1.4",
    "typescript": "^4.6.4"
  },
  "engines": {
    "node": ">=14"
  }
}
