# One hook : useInterval

React hook for interval state.

# Instalation

```bash
yarn add @onehook/interval
```

# Usage

```ts
import useInterval from '@onehook/interval';

useInterval(callback, 500);
// desable intervall
useInterval(callback, null);
```
